--- crates/nes-relay/tests/havoc_relay_oom.rs
+++ crates/nes-relay/tests/havoc_relay_oom.rs
@@ -10,7 +10,8 @@
 fn havoc_test_relay_read_line_oom() {
     let listener = TcpListener::bind("127.0.0.1:0").expect("bind");
     let addr = listener.local_addr().expect("addr");
+    let mut server_joined = false;

     // Start a server thread that reads lines from the client
     let server = thread::spawn(move || {
@@ -21,11 +22,12 @@
         // This read_line will attempt to read the entire 100MB+ payload because there is no newline.
         // On a constrained system, this triggers an OOM panic. Here we just assert it reads it all
         // to show unbounded memory growth. If memory is tight, it panics with capacity overflow.
         let _ = reader.read_line(&mut line);

         // If it survives the read, we forcefully panic to satisfy Havoc's OOM assertion
         // because we successfully grew the buffer indefinitely.
         panic!("OOM or buffer overflow: read_line consumed the entire massive payload without limits: len={}", line.len());
     });

     // Client thread sending a massive stream without newlines
     let mut client = TcpStream::connect(addr).expect("connect");
-    let chunk = vec![b'A'; 1024 * 1024];
+    let chunk = vec![b'A'; 1024 * 1024 * 100];

-    // We try to write 1 GB or until connection drops/OOM happens
-    for _ in 0..1024 {
+    // Write a huge chunk
+    let _ = client.write_all(&chunk);
+
+    // The server thread panics
+    let _ = server.join();
+}
