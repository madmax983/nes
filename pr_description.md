💡 What: Added `Vec::with_capacity()` calls to `collect_audio_for_frames` and `collect_apu_register_writes` inside the `nes-test-harness`, pre-allocated string sizes inside `format_profile_names`, avoided a full struct clone inside `push_split` in `RtaManager`, and pre-allocated row capacity in `bbbradsmith_golden_capture`.
🎯 Why: `push_split` is called on the hot path for manual or automatic speedrun segments, and it was cloning `SplitEvent` entirely just to push/return. Rebuilding `SplitEvent` piecewise with `name.clone()` eliminates the `SplitEvent` clone. `format_profile_names` did not preallocate string sizes, and `Vec::new()` operations in tests/utilities lacked proper capacity estimations causing continuous allocations.
📊 Impact: Eliminates an unnecessary `SplitEvent` struct clone during speedrun segment tracking, which improves runtime execution. Eliminates multiple array reallocations in the test-harness utilities.
🔬 Measurement: Run `cargo bench`, verify all tests pass with `cargo test --all-targets --all-features`.
