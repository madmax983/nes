## 2025-06-18 - The FME-7 Module Documentation
**Confusion:** The `crates/nes-core/src/mapper/fme7.rs` file had missing executable doctests and was throwing an error from intra-doc linking due to `DOTS_PER_CPU_CYCLE` being private.
**Clarification:** I added `/// # Examples` code block execution tests for `from_prg_chr`, `chr_window`, `chr_writable`, `sync_chr_ram_from_ppu_window`, `mirroring`, `irq_pending`, `on_ppu_dot`, `read_prg_ram`, `write_prg_ram`, `read_prg`, `write_prg`. I also fixed the `rom` visibility in `crates/nes-core/src/lib.rs` and replaced `[`DOTS_PER_CPU_CYCLE`]` with ``` `DOTS_PER_CPU_CYCLE` ``` to fix the documentation warnings.
