## 2024-05-24 - API and CPU Coverage Improvements
**Learning:** Found several untested instructions and branches in the NES CPU emulation core mapping logic and APU frame sequencer. The core test coverage was significantly boosted by adding direct `#[test]`s to `crates/nes-core/src/cpu/engine.rs` to exercise missing instruction variants. We also had to work around a debug assertion in `apply_delta()` logic when feeding mismatched delta structures to mapper objects in `api.rs`.
**Action:** Continued systematically isolating missing lines and writing straightforward state validations testing registers, memory writes, and status flags. For subsequent coverage, the focus on direct CPU integration trace step testing proves most effective.
