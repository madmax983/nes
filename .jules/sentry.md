## 2026-05-10 - Unreachable Timeline Prune Logic and EMA Math Mutants
**Learning:** Found mathematically dead code in `nes-rewind/src/timeline.rs` `prune` logic (an inner `while` loop that could never be entered because orphaned deltas would have already been absorbed by the outer `if` condition), as well as complex boundaries around the `KeyframePolicy::should_promote` and `ema_step` fixed-point math where `<=` equivalents were hidden by overlapping validation criteria.
**Action:** Always test exactly the value of threshold boundaries to expose operator mutation bugs (e.g. `>` -> `>=`), and aggressively delete dead `while`/`if` blocks found by equivalent mutants. Kept mathematical boundary testing strictly inside the `#[cfg(test)] mod tests` bounds to maintain idiomatic code hygiene without bleeding stream-of-consciousness comments into the source tree.
