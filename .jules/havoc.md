## 2025-06-18 - Havoc tests for netplay, relay, and MCP
**Learning:** Found unbounded allocations (`saved_states` static, `RoomState`), math overflow in `nes-relay` via random input delay simulation without bounds checks, and unsafe unwrapping/panic loops via missed asserts in `nes-netplay` rollback ingestion bounds.
**Action:** Added targeted integration tests `crates/nes-mcp/tests/havoc_mcp_save_state.rs`, `crates/nes-netplay/tests/havoc_rollback.rs`, modified `crates/nes-relay/tests/havoc.rs`, and appended inline test `havoc_test_sample_delay_ms_does_not_panic_with_extreme_values` in `crates/nes-relay/src/main.rs`.
