1. Add explicit file tests (`crates/nes-core/tests/sentinel_*.rs`) covering `ppm`, `serde_array`, `bus`, `cheat_codes`, and `status`.
2. Verify all test additions run successfully (`cargo test -p nes-core`).
3. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
4. Submit PR.
