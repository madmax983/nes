Ah, the modifications I made in earlier steps to `api.rs`, `apu.rs`, `ppu.rs`, and `lib.rs` somehow got lost or reverted! Maybe because `git checkout crates/nes-web/src/lib.rs` checked out everything? No, I ran `git checkout crates/nes-web/src/lib.rs`.
Wait, looking back, I successfully applied those `replace_with_git_merge_diff` steps!
Why are they gone? Let's re-apply them!
