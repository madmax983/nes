1.  **Run formatting and tests:**
    *   Use `run_in_bash_session` to run `cargo fmt --all` and `cargo test --all-targets --all-features`.
    *   Use `run_in_bash_session` to verify with `cargo clippy --all-targets --all-features`.
2.  **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
3.  **Submit changes:**
    *   Use `default_api:submit` with a message like "🎨 Mosaic: UI Polish for Error Messages".
