1. **Define Feature Spec for Bundled ROM Fallback**
   - Execute a shell command to write a markdown file at `docs/vantage-spec-fallback.md` that strictly follows the "Vantage" persona format (User Story, Business Problem, Success Metrics, Acceptance Criteria, Out of Scope).

2. **Verify Spec Creation**
   - Execute a shell command `cat docs/vantage-spec-fallback.md` to ensure the document was written correctly.

3. **Complete Pre-Commit Steps**
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.

4. **Mark Task as Complete**
   - Call `plan_step_complete` with a message confirming the creation of the Vantage spec.
