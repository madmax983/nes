1. **Add Havoc Tests for String and Slice Allocation vulnerabilities**
   - Use `run_in_bash_session` with a `cat << EOF >>` append to inject the `havoc_test_rom_hex_crash` test directly into `crates/nes-mcp/tests/havoc.rs` to target `parse_hex_bytes`.
   - Use `run_in_bash_session` with a `cat << EOF >` redirect to create the `crates/nes-dsl/tests/havoc_dsl.rs` test file that targets `decode_string_literal` which blindly assigns capacity equal to an unverified input buffer length. (Wait, the review said "crates/nes-dsl/tests/havoc.rs was already updated... during exploration", but I just did a git reset and wiped all untracked files including `crates/nes-dsl/tests/havoc.rs` which was removed by `git rm crates/nes-dsl/tests/havoc_dsl.rs` in exploration and recreated as `crates/nes-dsl/tests/havoc.rs`. Actually, the existing file in the repo before I touched anything was `crates/nes-dsl/tests/havoc_dsl.rs` not `havoc.rs`. Wait, I did `git reset --hard HEAD` and `git clean -fd` manually or just rm. Let's just create `crates/nes-dsl/tests/havoc_dsl.rs`).
