[package]
name = "nes-mcp"
version = "0.1.0"
edition = "2024"

[dependencies]
nes-core = { path = "../nes-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1.0.0"
