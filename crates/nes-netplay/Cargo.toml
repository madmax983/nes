[package]
name = "nes-netplay"
version = "0.1.0"
edition = "2024"

[dependencies]
nes-core = { path = "../nes-core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1.11.0"
serde_json = "1"
