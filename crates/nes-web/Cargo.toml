[package]
name = "nes-web"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nes-core = { path = "../nes-core" }
wasm-bindgen = "0.2"

[dev-dependencies]
wasm-bindgen-test = "0.2"
