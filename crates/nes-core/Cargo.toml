[package]
name = "nes-core"
version = "0.1.0"
edition = "2024"

[features]
nova = []
tas = []

[dependencies]
itoa = "1.0.17"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
arbitrary = "1.4.2"
loom = "0.7.2"
proptest = "1.0.0"
serde_json = "1"
