[package]
name = "nes-ai"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"

[dependencies]
burn-autodiff = { version = "0.19.1", default-features = false, features = ["std"] }
burn-core = { version = "0.19.1", default-features = false, features = ["std"] }
burn-ndarray = { version = "0.19.1", default-features = false, features = ["std"] }
burn-nn = { version = "0.19.1", default-features = false, features = ["std"] }
burn-tensor = { version = "0.19.1", default-features = false, features = ["std"] }
nes-core = { path = "../nes-core", features = ["tas"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
