plan = """1. In `nes-core/src/cpu/engine.rs`, the `decode_unofficial_rmw_addressing` function is over 80 lines long and contains a large `match` statement. Extracted the inner logic of each branch into smaller helper functions. This has been completed.
2. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
3. Submit the change with PR title `⚒️ Forge: Refactor decode_unofficial_rmw_addressing` and required description sections.
"""
